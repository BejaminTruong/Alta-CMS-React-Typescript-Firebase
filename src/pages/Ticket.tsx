import React, { FC } from 'react'

type Props = {}

const Ticket: FC = (props: Props) => {
  return (
    <div className="mainContainer">
    <h1 className="text-5xl font-bold">Danh sách vé</h1>
  </div>
  )
}

export default Ticket