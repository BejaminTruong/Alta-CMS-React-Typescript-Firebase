import React, { FC } from "react";

type Props = {};

const Invoice: FC = (props: Props) => {
  return (
    <div className="mainContainer">
      <h1 className="text-5xl font-bold">Đối soát vé</h1>
    </div>
  );
};

export default Invoice;
