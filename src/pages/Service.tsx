import React, { FC } from "react";

type Props = {};

const Service: FC = (props: Props) => {
  return (
    <div className="mainContainer">
      <h1 className="text-5xl font-bold">Danh sách gói vé</h1>
    </div>
  );
};

export default Service;
