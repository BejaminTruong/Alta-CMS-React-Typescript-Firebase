import { FC } from "react";

type Props = {};

const Home: FC = (props: Props) => {
  return <div className="mainContainer">
    <h1 className="text-5xl font-bold">Thống kê</h1>
  </div>;
};

export default Home;
